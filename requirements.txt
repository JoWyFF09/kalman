# Dependencias del despliegue publico, es decir, de demo_app.py.
#
# Por que este fichero es tan corto
# ---------------------------------
# Streamlit Community Cloud no permite elegir el nombre del fichero de
# dependencias: busca siempre "requirements.txt" en la raiz del repositorio.
# Como lo unico que se despliega en abierto es la demo, este fichero contiene
# exactamente lo que la demo necesita y nada mas.
#
# Cuanto menos lleve el despliegue publico, menos tarda en arrancar y menos
# superficie hay que vigilar. La demo no habla con la base de datos ni con
# Stripe, asi que no instala sus paquetes ni pide sus credenciales.
#
# Para desarrollar con el proyecto completo, incluidas API, base de datos y
# pagos, no uses este fichero. Usa los extras declarados en pyproject.toml:
#
#     pip install -e ".[web,api,db,billing,dev]"
#
# Ya no aparece TensorFlow. La red neuronal anterior no aportaba capacidad de
# deteccion y si unos 600 MB de descarga y minutos de arranque en frio.

pandas>=2.1
numpy>=1.26
streamlit>=1.40
openpyxl>=3.1
fpdf2>=2.8
