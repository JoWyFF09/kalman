# Dependencias del despliegue web completo.
#
# Para instalar sólo el motor, que es lo que se ejecuta en un proceso por
# lotes o en una prueba, basta con:  pip install pandas numpy
#
# Ya no aparece TensorFlow. La red neuronal que había antes no aportaba
# capacidad de detección y sí unos 600 MB de descarga, varios minutos de
# arranque en frío y un consumo de memoria que obliga a pagar servidor.

# Núcleo
pandas>=2.1
numpy>=1.26

# Interfaz web
streamlit>=1.40
openpyxl>=3.1
fpdf2>=2.8

# API
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-multipart>=0.0.12

# Base de datos
psycopg[binary]>=3.2
psycopg-pool>=3.2

# Pagos
stripe>=11.0
