# Copia este fichero a .env y rellénalo. El fichero .env NUNCA se sube al
# repositorio: está en .gitignore y debe seguir estándolo.

# ------------------------------------------------------------------- entorno
# development o production. En production se exige HTTPS y clave de Stripe live.
KALMAN_ENV=development

# URL pública de la aplicación. Stripe redirige aquí tras el pago.
APP_URL=http://localhost:8501

# --------------------------------------------------------------- base de datos
# Cadena de conexión de Supabase.
#
# IMPORTANTE: usa el "Session pooler", no la "Direct connection".
#
# Supabase publica la conexión directa, la del servidor db.<ref>.supabase.co,
# únicamente por IPv6. Muchas conexiones domésticas españolas no tienen IPv6,
# así que el nombre no se resuelve y el error que sale es
# "failed to resolve host", que parece un fallo de contraseña y no lo es.
#
# El pooler sí tiene IPv4 y funciona en todas partes. Cambian dos cosas:
#   el usuario es  postgres.<ref>  en lugar de  postgres
#   el servidor es aws-N-<region>.pooler.supabase.com
# La N puede ser 0 o 1 según cuándo se creara el proyecto. Cópiala del panel.
#
# Puerto 5432 es modo sesión y es el adecuado aquí, porque psycopg usa
# sentencias preparadas y el modo transacción del puerto 6543 no las admite.
DATABASE_URL=postgresql://postgres.REF:CONTRASENA@aws-1-eu-north-1.pooler.supabase.com:5432/postgres

# ------------------------------------------------------- clave criptográfica
# Clave de seudonimización. Si se pierde, los seudónimos ya emitidos dejan de
# poder relacionarse con nuevos lotes. Si se filtra, la seudonimización deja de
# proteger. Guárdala como guardarías la contraseña de la base de datos.
#
# Genérala con:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
KALMAN_PSEUDONYM_KEY=

# ---------------------------------------------------------------------- Stripe
# Clave secreta. sk_test_ para pruebas, sk_live_ para cobros reales.
STRIPE_SECRET_KEY=

# Secreto del webhook. Lo da Stripe al crear el endpoint en el panel.
# Sin él no se puede verificar la firma y cualquiera podría regalarse un plan.
STRIPE_WEBHOOK_SECRET=

# Identificadores de precio creados en Stripe. El importe vive en Stripe, no
# en el código, para que cambiar un precio no requiera un despliegue.
#
# Estos tres ya están creados en la cuenta de producción, en euros y con
# facturación mensual:
#   Kalman Starter    29 EUR/mes
#   Kalman Growth     99 EUR/mes
#   Kalman Scale     399 EUR/mes
STRIPE_PRICE_STARTER=price_1UDpAl2VrhLoHuYyMR6CJxpE
STRIPE_PRICE_GROWTH=price_1UDpBE2VrhLoHuYyfZhS5USA
STRIPE_PRICE_SCALE=price_1UDpBZ2VrhLoHuYy4heZHflc

# -------------------------------------------------------------------- soporte
SUPPORT_EMAIL=soporte@ejemplo.com

# --------------------------------------------------------------------- límites
MAX_UPLOAD_MB=50
SESSION_TTL_HOURS=12

# ---------------------------------------------------------------------- correo
# Correo transaccional: recuperar contrasena y confirmar la direccion.
# Es OPCIONAL. Sin esto la aplicacion funciona igual, pero esas dos pantallas
# avisan de que no estan disponibles en lugar de fallar.
#
# HAY DOS VIAS Y NO SON EQUIVALENTES
# ----------------------------------
# Por API, sobre HTTPS. Es la que hay que usar en un alojamiento gestionado
# como Render, porque casi todos cierran la salida por los puertos de correo
# para que no se usen como plataforma de envio masivo. El sintoma cuando esta
# cerrado es "[Errno 101] Network is unreachable", que parece un problema de
# credenciales y no lo es.
#
# Proveedor: Brevo. Plan gratuito de 300 correos al dia y, lo importante
# cuando todavia no tienes dominio propio, permite verificar una sola
# direccion de remitente y enviar a cualquiera.
#   brevo.com -> crear cuenta -> SMTP & API -> API Keys -> generar
#   Y en Senders, verifica tu direccion de correo.
EMAIL_API_KEY=
EMAIL_FROM=

# Por SMTP. Sirve en tu portatil o en un servidor propio, NO en Render.
# Con Gmail hay que generar una "contrasena de aplicacion", que exige tener la
# verificacion en dos pasos activada.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
